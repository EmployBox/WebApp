[Unit]
Description=The EmployBox HTTP API
After=syslog.target

[Service]
User=spring
Type=idle
ExecStart=/usr/bin/java -jar /var/spring/web_api.jar --server.port=8080 --spring.profiles.active=gcp
SuccessExitStatus=143
Restart=always

[Install]
WantedBy=multi-user.target